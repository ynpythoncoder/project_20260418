[project]
name = "project-20260418"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "dev-tools",
]

[tool.uv.sources]
dev-tools = { path = "../dev_tools", editable = true }
